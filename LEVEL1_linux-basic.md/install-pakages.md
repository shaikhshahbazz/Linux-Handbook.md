## Install packages

## Git → install Git
  sudo yum install git -y
  sudo git --version

## Nginx → install Nginx
  sudo yum install nginx -y
  sudo nginx --version
  
## Java → install java
  sudo yum install java -y
  sudo java --version
